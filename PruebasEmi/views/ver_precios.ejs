<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Precios de Libros</title>
    <link rel="stylesheet" href="/Styles.css" />
    <link rel="stylesheet" href="/PruebasEmi/views/books.css" />
    <style>
        body {
            background: linear-gradient(to right, #2c3e50, #3498db); /* Igual que menú principal */
            color: #fff;
        }
        .precios-container {
            max-width: 900px;
            margin: 40px auto;
            background: rgba(255,255,255,0.10);
            border-radius: 12px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.10);
            padding: 30px 40px;
        }
        h1 {
            text-align: center;
            color: #5dade2;
            margin-bottom: 30px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background: transparent;
        }
        th, td {
            padding: 14px 10px;
            text-align: left;
            border-bottom: 1px solid #5dade2;
        }
        th {
            background: #5dade2;
            color: #fff;
        }
        tr:hover {
            background: rgba(255,255,255,0.08);
        }
    </style>
</head>
<body>
    <div class="precios-container">
        <h1>Todos los Libros y sus Precios</h1>
        <table>
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Autor</th>
                    <th>Categoría</th>
                    <th>Precio</th>
                </tr>
            </thead>
            <tbody>
                <% libros.forEach(libro => { %>
                    <tr>
                        <td><%= libro.nombre %></td>
                        <td><%= libro.autor %></td>
                        <td><%= libro.categoria %></td>
                        <td>$250 MXN</td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
